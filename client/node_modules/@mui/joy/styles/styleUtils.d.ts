import { Theme, SxProps } from './types';
/**
 * internal utility
 */
export declare const resolveSxValue: ({ theme, ownerState }: {
    theme: Theme;
    ownerState: {
        sx?: SxProps;
    };
}, key: string, defaultValue?: string | number) => any;
